<?php
//seesion of the last user
$_SESSION["lastUser"] = $row[0];
//seesion count variable
	$sessionCount ;

	for ($sessionCount=0; $sessionCount <=3; $sessionCount++) { 
		if ($_SESSION["lastUser"] = $row[0];) {
			
		}else{

		}
	}

	?>